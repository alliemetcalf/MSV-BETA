rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY DEBUGGING RULE:
    // Allow all reads and writes to all files.
    // This is insecure and should be locked down after testing.
    match /{allPaths=**} {
      allow read, write;
    }
  }
}
