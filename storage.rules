rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write access to all paths for all users,
      // including unauthenticated ones.
      allow read, write;
    }
  }
}
