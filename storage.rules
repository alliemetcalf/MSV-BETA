rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // WARNING: These rules are insecure and for testing only.
      // They allow anyone to read, write, and delete any file.
      allow read, write;
    }
  }
}
