rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // WARNING: This is a temporary, insecure rule for debugging.
      // It allows anyone to read, write, and delete any file.
      allow read, write;
    }
  }
}
