rules_version = '2';

service firebase.storage {
  match /b/msv4.appspot.com/o {
    // Allow read and write access to any authenticated user for any file.
    // This is for debugging purposes and should be secured later.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
