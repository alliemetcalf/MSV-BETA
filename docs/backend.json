{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's public profile information.",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "bio": {
          "type": "string",
          "description": "A short biography for the user."
        }
      }
    },
    "DoorCode": {
      "title": "DoorCode",
      "type": "object",
      "description": "Represents a door code for a specific property.",
      "properties": {
        "location": {
          "type": "string",
          "description": "The physical location of the door (e.g., 'Front Door', 'Garage')."
        },
        "adminProgrammingCode": {
          "type": "string",
          "description": "The admin or programming code for the lock."
        },
        "guestCode": {
          "type": "string",
          "description": "The guest code for the lock."
        },
        "doorLockType": {
          "type": "string",
          "description": "The type of the door lock."
        },
        "property": {
          "type": "string",
          "description": "The property the door code belongs to."
        },
        "lastChanged": {
          "type": "string",
          "description": "The date and time the door code was last changed.",
          "format": "date-time"
        }
      },
      "required": [
        "location",
        "doorLockType",
        "property"
      ]
    },
    "LockTypes": {
      "title": "LockTypes",
      "type": "object",
      "description": "A list of available door lock types for the platform.",
      "properties": {
        "options": {
          "type": "array",
          "description": "An array of door lock type names.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "options"
      ]
    },
    "Properties": {
      "title": "Properties",
      "type": "object",
      "description": "A list of available properties for the platform.",
      "properties": {
        "options": {
          "type": "array",
          "description": "An array of property names.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "options"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "collections": [
      {
        "path": "/users/{userId}",
        "schema": {
          "$ref": "#/entities/UserProfile"
        },
        "description": "Stores the public profile for a user."
      },
      {
        "path": "/users/{userId}/doorCodes",
        "schema": {
          "$ref": "#/entities/DoorCode"
        },
        "description": "Stores door codes for a specific user."
      },
      {
        "path": "/siteConfiguration/lockTypes",
        "schema": {
          "$ref": "#/entities/LockTypes"
        },
        "description": "Stores the list of available door lock types."
      },
      {
        "path": "/siteConfiguration/properties",
        "schema": {
          "$ref": "#/entities/Properties"
        },
        "description": "Stores the list of available properties."
      }
    ]
  }
}
