
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's public profile information and role.",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "bio": {
          "type": "string",
          "description": "A short biography for the user."
        },
        "role": {
          "type": "string",
          "description": "The user's role in the system.",
          "enum": [
            "superadmin",
            "manager",
            "contractor",
            "user"
          ]
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        }
      },
      "required": [
        "role",
        "email"
      ]
    },
    "DoorCode": {
      "title": "DoorCode",
      "type": "object",
      "description": "Represents a door code for a specific property.",
      "properties": {
        "location": {
          "type": "string",
          "description": "The physical location of the door (e.g., 'Front Door', 'Garage')."
        },
        "code": {
          "type": "string",
          "description": "The primary code for the lock."
        },
        "adminProgrammingCode": {
          "type": "string",
          "description": "The admin or programming code for the lock."
        },
        "guestCode": {
          "type": "string",
          "description": "The guest code for the lock."
        },
        "doorLockType": {
          "type": "string",
          "description": "The name of the door lock type."
        },
        "property": {
          "type": "string",
          "description": "The property the door code belongs to."
        },
        "lastChanged": {
          "type": "string",
          "description": "The date and time the door code was last changed.",
          "format": "date-time"
        }
      },
      "required": [
        "location",
        "doorLockType",
        "property"
      ]
    },
    "LockType": {
      "title": "LockType",
      "type": "object",
      "description": "Represents a single door lock type with instructions.",
      "properties": {
        "id": {
          "type": "string",
          "description": "A unique identifier for the lock type."
        },
        "name": {
          "type": "string",
          "description": "The name of the door lock type."
        },
        "textInstructions": {
          "type": "string",
          "description": "Text-based instructions for the lock."
        },
        "instructionImageUrl": {
          "type": "string",
          "description": "A URL to an image of the instruction sheet."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "LockTypesConfiguration": {
      "title": "LockTypesConfiguration",
      "type": "object",
      "description": "A list of available door lock types for the platform.",
      "properties": {
        "types": {
          "type": "array",
          "description": "An array of door lock type objects.",
          "items": {
            "$ref": "#/entities/LockType"
          }
        }
      }
    },
    "Property": {
      "title": "Property",
      "type": "object",
      "description": "Represents a single property with its details.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the property."
        },
        "address": {
          "type": "string",
          "description": "The full address of the property."
        },
        "description": {
          "type": "string",
          "description": "A description of the property."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL of the property's photo."
        },
        "mortgage": {
          "type": "number",
          "description": "The monthly mortgage payment for the property."
        },
        "averageExpensePerTenant": {
          "type": "number",
          "description": "An estimated average monthly expense per tenant for this property."
        },
        "active": {
          "type": "boolean",
          "description": "Whether the property is currently active."
        }
      },
      "required": [
        "name",
        "active"
      ]
    },
    "Tenant": {
      "title": "Tenant",
      "type": "object",
      "description": "Represents a single tenant with their contact details and property assignment.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the tenant."
        },
        "phone": {
          "type": "string",
          "description": "The tenant's phone number."
        },
        "email": {
          "type": "string",
          "description": "The tenant's email address.",
          "format": "email"
        },
        "property": {
          "type": "string",
          "description": "The property the tenant is residing in."
        },
        "room": {
          "type": "string",
          "description": "The room number or identifier for the tenant."
        },
        "rent": {
          "type": "number",
          "description": "The monthly rent amount for the tenant."
        },
        "deposit": {
          "type": "number",
          "description": "The security deposit amount paid by the tenant."
        },
        "notes": {
          "type": "string",
          "description": "General notes about the tenant."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL of the tenant's photo."
        },
        "active": {
          "type": "boolean",
          "description": "Whether the tenant is currently active."
        },
        "leaseEffective": {
          "type": "string",
          "format": "date-time",
          "description": "The date the lease becomes effective."
        },
        "leaseEnded": {
          "type": "string",
          "format": "date-time",
          "description": "The date the lease ends."
        },
        "noticeReceivedDate": {
          "type": "string",
          "format": "date-time",
          "description": "The date the 30-day notice was received from the tenant."
        },
        "pendingMove": {
          "type": "object",
          "description": "Details for a tenant's scheduled future move.",
          "properties": {
            "newProperty": {
              "type": "string"
            },
            "newRoom": {
              "type": "string"
            },
            "moveDate": {
              "type": "string",
              "format": "date-time"
            },
            "newRent": {
              "type": "number"
            },
            "newDeposit": {
              "type": "number"
            }
          }
        }
      },
      "required": [
        "name",
        "property",
        "active",
        "rent"
      ]
    },
    "Expense": {
      "title": "Expense",
      "type": "object",
      "description": "Represents a single business expense.",
      "properties": {
        "date": {
          "type": "string",
          "format": "date-time",
          "description": "The date the expense was incurred."
        },
        "amount": {
          "type": "number",
          "description": "The monetary value of the expense."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the expense."
        },
        "category": {
          "type": "string",
          "description": "The category of the expense (e.g., 'Repairs', 'Utilities')."
        },
        "vendor": {
          "type": "string",
          "description": "The vendor or supplier for the expense."
        },
        "property": {
          "type": "string",
          "description": "The property associated with the expense, if any."
        },
        "room": {
          "type": "string",
          "description": "The specific room associated with the expense, if any."
        },
        "receiptUrl": {
          "type": "string",
          "description": "URL of the uploaded receipt image."
        }
      },
      "required": [
        "date",
        "amount",
        "description",
        "category"
      ]
    },
    "ExpenseCategory": {
      "title": "ExpenseCategory",
      "type": "object",
      "description": "Represents a single category for expenses.",
      "properties": {
        "id": {
          "type": "string",
          "description": "A unique identifier for the category."
        },
        "name": {
          "type": "string",
          "description": "The name of the expense category."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "ExpenseCategoriesConfiguration": {
      "title": "ExpenseCategoriesConfiguration",
      "type": "object",
      "description": "A list of available expense categories.",
      "properties": {
        "categories": {
          "type": "array",
          "description": "An array of expense category objects.",
          "items": {
            "$ref": "#/entities/ExpenseCategory"
          }
        }
      }
    },
    "Vendor": {
      "title": "Vendor",
      "type": "object",
      "description": "Represents a single vendor or supplier.",
      "properties": {
        "id": {
          "type": "string",
          "description": "A unique identifier for the vendor."
        },
        "name": {
          "type": "string",
          "description": "The name of the vendor."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "VendorsConfiguration": {
      "title": "VendorsConfiguration",
      "type": "object",
      "description": "A list of available vendors.",
      "properties": {
        "vendors": {
          "type": "array",
          "description": "An array of vendor objects.",
          "items": {
            "$ref": "#/entities/Vendor"
          }
        }
      }
    },
    "RentPayment": {
      "title": "RentPayment",
      "type": "object",
      "description": "Represents a single rent payment made by a tenant.",
      "properties": {
        "tenantId": {
          "type": "string",
          "description": "The ID of the tenant who made the payment."
        },
        "tenantName": {
          "type": "string",
          "description": "The name of the tenant for display purposes."
        },
        "property": {
          "type": "string",
          "description": "The property for which rent was paid."
        },
        "room": {
          "type": "string",
          "description": "The specific room associated with this payment."
        },
        "date": {
          "type": "string",
          "format": "date-time",
          "description": "The date the rent payment was received."
        },
        "amount": {
          "type": "number",
          "description": "The monetary value of the payment."
        },
        "category": {
          "type": "string",
          "description": "The category of income (e.g., 'Rent', 'Late Fee')."
        },
        "paymentMethod": {
          "type": "string",
          "description": "The method of payment (e.g., 'Zelle', 'Cash')."
        },
        "notes": {
          "type": "string",
          "description": "Additional notes about the payment."
        }
      },
      "required": [
        "tenantId",
        "tenantName",
        "property",
        "room",
        "date",
        "amount",
        "paymentMethod",
        "category"
      ]
    },
    "IncomeType": {
      "title": "IncomeType",
      "type": "object",
      "description": "Represents a single category for income.",
      "properties": {
        "id": {
          "type": "string",
          "description": "A unique identifier for the income type."
        },
        "name": {
          "type": "string",
          "description": "The name of the income type (e.g., Rent, Late Fee)."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "IncomeTypesConfiguration": {
      "title": "IncomeTypesConfiguration",
      "type": "object",
      "description": "A list of available income types.",
      "properties": {
        "types": {
          "type": "array",
          "description": "An array of income type objects.",
          "items": {
            "$ref": "#/entities/IncomeType"
          }
        }
      }
    },
    "PaymentMethod": {
      "title": "PaymentMethod",
      "type": "object",
      "description": "Represents a single payment method.",
      "properties": {
        "id": {
          "type": "string",
          "description": "A unique identifier for the payment method."
        },
        "name": {
          "type": "string",
          "description": "The name of the payment method (e.g., Zelle, Cash)."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "PaymentMethodsConfiguration": {
      "title": "PaymentMethodsConfiguration",
      "type": "object",
      "description": "A list of available payment methods.",
      "properties": {
        "methods": {
          "type": "array",
          "description": "An array of payment method objects.",
          "items": {
            "$ref": "#/entities/PaymentMethod"
          }
        }
      }
    },
    "ContractorTask": {
      "title": "ContractorTask",
      "type": "object",
      "description": "Represents a single task assigned to a contractor.",
      "properties": {
        "assignedTo": {
          "type": "string",
          "description": "The UID of the user (contractor) assigned to the task."
        },
        "assignedToName": {
          "type": "string",
          "description": "The display name of the user (contractor) assigned to the task."
        },
        "invoiceNumber": {
          "type": "number",
          "description": "The invoice number this task belongs to."
        },
        "dateOfTask": {
          "type": "string",
          "format": "date-time",
          "description": "The date the task was performed."
        },
        "taskType": {
          "type": "string",
          "description": "The category of the task."
        },
        "property": {
          "type": "string",
          "description": "The property where the task was performed."
        },
        "room": {
          "type": "string",
          "description": "The specific room number or identifier."
        },
        "description": {
          "type": "string",
          "description": "A description of the task."
        },
        "hoursWorked": {
          "type": "number",
          "description": "The number of hours worked."
        },
        "hourlyRate": {
          "type": "number",
          "description": "The hourly rate for the task."
        },
        "mileage": {
          "type": "number",
          "description": "Mileage for the task."
        },
        "mileageRate": {
          "type": "number",
          "description": "The rate per mile."
        },
        "totalPaid": {
          "type": "number",
          "description": "The total amount paid for this specific task."
        },
        "datePaid": {
          "type": "string",
          "format": "date-time",
          "description": "The date the task was paid."
        },
        "totalInvoiceAmount": {
          "type": "number",
          "description": "The total amount for the entire invoice."
        },
        "status": {
          "type": "string",
          "description": "The status of the task.",
          "enum": [
            "Pending",
            "In Progress",
            "Completed",
            "Paid"
          ]
        }
      },
      "required": [
        "dateOfTask",
        "taskType",
        "property",
        "status"
      ]
    },
    "TaskType": {
      "title": "TaskType",
      "type": "object",
      "description": "Represents a single predefined type for a contractor task.",
      "properties": {
        "id": {
          "type": "string",
          "description": "A unique identifier for the task type."
        },
        "name": {
          "type": "string",
          "description": "The name of the task type."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "ContractorTasksConfiguration": {
      "title": "ContractorTasksConfiguration",
      "type": "object",
      "description": "Configuration for contractor tasks, including global mileage rate and predefined task types.",
      "properties": {
        "mileageRate": {
          "type": "number",
          "description": "The global mileage rate to be applied to tasks."
        },
        "taskTypes": {
          "type": "array",
          "description": "An array of predefined task type objects.",
          "items": {
            "$ref": "#/entities/TaskType"
          }
        }
      }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores the public profile for a user, including their role."
    },
    "/doorCodes/{doorCodeId}": {
      "schema": {
        "$ref": "#/entities/DoorCode"
      },
      "description": "Stores all door codes for all properties."
    },
    "/siteConfiguration/lockTypes": {
      "schema": {
        "$ref": "#/entities/LockTypesConfiguration"
      },
      "description": "Stores the list of available door lock types with their instructions."
    },
    "/siteConfiguration/expenseCategories": {
      "schema": {
        "$ref": "#/entities/ExpenseCategoriesConfiguration"
      },
      "description": "Stores the list of available expense categories."
    },
    "/siteConfiguration/vendors": {
      "schema": {
        "$ref": "#/entities/VendorsConfiguration"
      },
      "description": "Stores the list of available vendors."
    },
    "/siteConfiguration/incomeTypes": {
      "schema": {
        "$ref": "#/entities/IncomeTypesConfiguration"
      },
      "description": "Stores the list of available income types."
    },
    "/siteConfiguration/paymentMethods": {
      "schema": {
        "$ref": "#/entities/PaymentMethodsConfiguration"
      },
      "description": "Stores the list of available payment methods."
    },
    "/siteConfiguration/contractorTasks": {
      "schema": {
        "$ref": "#/entities/ContractorTasksConfiguration"
      },
      "description": "Stores global settings for contractor tasks."
    },
    "/properties/{propertyId}": {
      "schema": {
        "$ref": "#/entities/Property"
      },
      "description": "Stores the detailed profile for each property."
    },
    "/tenants/{tenantId}": {
      "schema": {
        "$ref": "#/entities/Tenant"
      },
      "description": "Stores information about individual tenants."
    },
    "/expenses/{expenseId}": {
      "schema": {
        "$ref": "#/entities/Expense"
      },
      "description": "Stores all business expenses."
    },
    "/rentPayments/{paymentId}": {
      "schema": {
        "$ref": "#/entities/RentPayment"
      },
      "description": "Stores all recorded rent payments from tenants."
    },
    "/contractorTasks/{taskId}": {
      "schema": {
        "$ref": "#/entities/ContractorTask"
      },
      "description": "Stores all tasks for contractors."
    }
  }
}
